<section class="layer-construct">
  <div class="layer-wrapper">

    <!-- LEFT COLUMN (400x800) -->
    <div class="layer-card main-card">
      <div
        class="layer-bg"
        style="background-image:url('{{ section.settings.left_image_desktop | image_url }}')">
      </div>

      <div class="layer-content">
        <h3>{{ section.settings.left_heading }}</h3>

        <!-- TEXT + LIST FIX -->
        <div class="layer-text">
          {{ section.settings.left_text }}
        </div>
      </div>
    </div>

    <!-- RIGHT COLUMN -->
    <div class="layer-right">

      <!-- TOP FEATURE (800x400) -->
      {% if section.blocks.size > 0 %}
        {% assign first = section.blocks.first %}
        <div class="layer-card feature-wide">
          <div
            class="layer-bg"
            style="background-image:url('{{ first.settings.image_desktop | image_url }}')">
          </div>
          <div class="layer-overlay">
            <h4>{{ first.settings.heading }}</h4>
            <p>{{ first.settings.text }}</p>
          </div>
        </div>
      {% endif %}

      <!-- BOTTOM FEATURES (395x380) -->
      <div class="feature-grid">
        {% for block in section.blocks offset:1 limit:2 %}
          <div class="layer-card feature-square">
            <div
              class="layer-bg"
              style="background-image:url('{{ block.settings.image_desktop | image_url }}')">
            </div>
            <div class="layer-overlay">
              <h4>{{ block.settings.heading }}</h4>
              <p>{{ block.settings.text }}</p>
            </div>
          </div>
        {% endfor %}
      </div>

    </div>
  </div>
</section>

<style>
.layer-construct {
  
  padding: 80px 20px;
}

.layer-wrapper {
  max-width: 1200px;
  margin: auto;
  display: grid;
  grid-template-columns: 400px 1fr;
  gap: 24px;
}

/* BASE CARD */
.layer-card {
  position: relative;
  border-radius: 28px;
  overflow: hidden;
}

/* IMAGE */
.layer-bg {
  position: absolute;
  inset: 0;
  background-size: cover;
  background-position: center;
}

/* LEFT CARD */
.main-card {
  aspect-ratio: 1 / 2;
  display: flex;
  flex-direction: column;
  padding: 32px;
}

/* LEFT CONTENT — BOTTOM ALIGNED */
.layer-content {
  margin-top: auto;
  position: relative;
  z-index: 2;
}

.layer-content h3 {
  font-size: 26px;
  margin-bottom: 12px;
}

/* TEXT + LIST FIX */
.layer-text {
  font-size: 16px !important;
  line-height: 1.6;
}

.layer-text p {
  margin-bottom: 10px;
  font-size: 16px !important;
}

.layer-text ol {
  margin: 10px 0;
  padding-left: 18px;
}

.layer-text li {
  margin-bottom: 6px;
}

/* RIGHT COLUMN */
.layer-right {
  display: grid;
  grid-template-rows: auto 1fr;
  gap: 24px;
}

/* TOP FEATURE */
.feature-wide {
  aspect-ratio: 2 / 1;
}

/* BOTTOM FEATURES */
.feature-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 24px;
}

.feature-square {
  aspect-ratio: 1 / 1;
}

/* OVERLAY */
.layer-overlay {
  position: relative;
  z-index: 2;
  height: 100%;
  padding: 28px;
  display: flex;
  flex-direction: column;
  justify-content: flex-end;
  /* background: linear-gradient(to top, rgba(0,0,0,0.65), transparent); */
  color: #fff;
}

.layer-overlay:first-of-type h4,
.layer-overlay:first-of-type p {
  color: #000;
}




.layer-overlay h4 {
  font-size: 20px;
  margin-bottom: 8px;
}

.layer-overlay p {
  font-size: 16px !important;
  line-height: 1.5;
}

/* MOBILE */
@media (max-width: 900px) {
  .layer-wrapper {
    grid-template-columns: 1fr;
  }

  .feature-grid {
    grid-template-columns: 1fr;
  }

  .main-card,
  .feature-wide,
  .feature-square {
    aspect-ratio: auto;
    min-height: 420px;
  }
}
</style>

{% schema %}
{
  "name": "Layer Construct",
  "settings": [
    {
      "type": "image_picker",
      "id": "left_image_desktop",
      "label": "Left Image (Desktop)"
    },
    {
      "type": "text",
      "id": "left_heading",
      "label": "Left Heading",
      "default": "The 4-Layer Salon Construct"
    },
    {
      "type": "textarea",
      "id": "left_text",
      "label": "Left Text",
      "info": "You can use HTML (p, ol, li)"
    }
  ],
  "blocks": [
    {
      "type": "card",
      "name": "Feature Card",
      "settings": [
        {
          "type": "image_picker",
          "id": "image_desktop",
          "label": "Image"
        },
        {
          "type": "text",
          "id": "heading",
          "label": "Heading"
        },
        {
          "type": "textarea",
          "id": "text",
          "label": "Text"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Layer Construct Section",
      "blocks": [
        { "type": "card" },
        { "type": "card" },
        { "type": "card" }
      ]
    }
  ]
}
{% endschema %}
